<%= form_for(@wordup) do |f| %>
  <% if @wordup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wordup.errors.count, "error") %> prohibited this wordup from being saved:</h2>

      <ul>
      <% @wordup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :textinput %><br />
    <%= f.text_field :textinput %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
